/** @desc a character with basic info and a picture. */
const Character = `
  type Character {
    id: Int!
    name: String!
    description: String
    image: String
    rating: Int
  }
`;

export default Character;
